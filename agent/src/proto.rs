//! The generated protocol types.
//!
//! Regenerated from `spark_pulse/agent/agent.proto` on every build, so this
//! module has no hand-written content and never needs reviewing — what is
//! worth reviewing is the proto.

#![allow(clippy::all)]
tonic::include_proto!("sparkpulse.agent.v1");
